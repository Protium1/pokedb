<%= form_for(poke) do |f| %>
  <% if poke.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poke.errors.count, "error") %> prohibited this poke from being saved:</h2>

      <ul>
      <% poke.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :generation %>
    <%= f.number_field :generation %>
  </div>

  <div class="field">
    <%= f.label :acquired %>
    <%= f.check_box :acquired %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.file_field :picture, id: :poke_picture%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
